import { ArrowRight, FileSearch } from 'lucide-react'
import { Link } from 'react-router-dom'
import { ConfidenceBadge, MonospaceValue, SeverityBadge, StatusBadge, TimestampValue } from '..'
import type { FindingRecord } from '../../types'

export function FindingRow({ finding }: { finding: FindingRecord }) { return <li><Link to={`/findings/${finding.id}`} className="group grid min-w-0 gap-3 rounded-md border border-border-subtle bg-surface-base p-3 transition-colors hover:border-accent/50 hover:bg-surface-raised focus-visible:border-focus sm:grid-cols-[minmax(0,1fr)_auto]"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><FileSearch aria-hidden="true" className="size-4 shrink-0 text-accent" /><h3 className="font-semibold text-text-primary">{finding.title}</h3><StatusBadge status={finding.status} /><SeverityBadge severity={finding.severity} /><ConfidenceBadge confidence={finding.confidence} /></div><p className="mt-2 line-clamp-2 break-words text-sm leading-6 text-text-secondary">{finding.description}</p><div className="mt-3 flex flex-wrap gap-x-4 gap-y-2 text-xs text-text-muted"><span>Rule <MonospaceValue>{finding.rule_id}</MonospaceValue> v{finding.rule_version}</span><span>{finding.supporting_events.length} supporting events</span></div></div><div className="flex min-w-0 items-center gap-3 sm:justify-end"><div className="min-w-0 sm:text-right"><TimestampValue value={finding.updated_at} /><MonospaceValue className="mt-2 block max-w-48 truncate text-xs" title={finding.id}>{finding.id}</MonospaceValue></div><ArrowRight aria-hidden="true" className="size-4 shrink-0 text-text-muted transition-transform group-hover:translate-x-0.5 motion-reduce:transform-none" /></div></Link></li> }
