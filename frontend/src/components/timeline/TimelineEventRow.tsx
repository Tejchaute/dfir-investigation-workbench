import { Clock3 } from 'lucide-react'
import { ArtifactTypeBadge, MonospaceValue, TimestampValue } from '..'
import type { TimelineEventRecord } from '../../types'
import { TimelineEventTypeBadge } from './TimelineEventTypeBadge'
export function TimelineEventRow({ event, selected, onSelect }: { event: TimelineEventRecord; selected: boolean; onSelect: () => void }) { return <li className="relative pl-7"><span aria-hidden="true" className="absolute bottom-[-1rem] left-[7px] top-5 w-px bg-border-subtle" /><span aria-hidden="true" className="absolute left-0 top-4 grid size-4 place-items-center rounded-full border border-accent/60 bg-surface-base"><span className="size-1.5 rounded-full bg-accent" /></span><button type="button" aria-pressed={selected} onClick={onSelect} className="w-full min-w-0 rounded-md border border-border-subtle bg-surface-base p-3 text-left hover:border-accent/50 hover:bg-surface-raised focus-visible:ring-2 focus-visible:ring-focus sm:p-4"><div className="flex min-w-0 flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap gap-2"><TimelineEventTypeBadge type={event.event_type} /><ArtifactTypeBadge type={event.artifact_type} /></div><p className="mt-2 break-words text-sm font-medium leading-6 text-text-primary">{event.description}</p>{event.source_identifier && <MonospaceValue className="mt-1 block text-xs text-text-muted">{event.source_identifier}</MonospaceValue>}</div><div className="flex shrink-0 items-center gap-2 text-xs text-text-secondary"><Clock3 aria-hidden="true" className="size-3.5 text-accent" /><TimestampValue value={event.event_time} semantics={event.time_semantics} /></div></div></button></li> }
