import { Activity } from 'lucide-react'
import { Button, Dialog } from '..'
export function GenerateTimelineDialog({ open, onOpenChange, pending, onGenerate }: { open: boolean; onOpenChange: (open: boolean) => void; pending: boolean; onGenerate: () => void }) { return <Dialog open={open} onOpenChange={onOpenChange} title="Generate timeline" description="Normalize persisted artifact records into timeline observations."><div className="space-y-4"><p className="text-sm leading-6 text-text-secondary">The backend will generate deterministic TimelineEvents from existing ArtifactRecords. Evidence is not reparsed or modified, and no new forensic evidence is created.</p><p className="text-xs text-text-muted">Generation is synchronous. No progress percentage is inferred.</p><div className="flex justify-end gap-2"><Button variant="ghost" onClick={() => onOpenChange(false)} disabled={pending}>Cancel</Button><Button leadingIcon={<Activity aria-hidden="true" className="size-4" />} onClick={onGenerate} loading={pending}>{pending ? 'Generating timeline' : 'Generate timeline'}</Button></div></div></Dialog> }
