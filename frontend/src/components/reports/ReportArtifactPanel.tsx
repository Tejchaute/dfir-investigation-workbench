import { Download, FileArchive } from 'lucide-react'
import { Badge, Button, HashValue, MonospaceValue, TimestampValue } from '..'
import type { ReportArtifactRecord } from '../../types'
import { formatBytes } from '../../utils'

export function ReportArtifactPanel({ artifact, pending, onDownload }: { artifact: ReportArtifactRecord; pending: boolean; onDownload: () => void }) { return <article className="rounded-md border border-border-subtle bg-surface-inset p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><FileArchive aria-hidden="true" className="size-4 text-accent" /><h3 className="text-sm font-semibold text-text-primary">Rendered report artifact</h3><Badge>{artifact.format}</Badge></div><dl className="mt-3 grid gap-3 sm:grid-cols-2"><Field label="Artifact ID"><MonospaceValue>{artifact.id}</MonospaceValue></Field><Field label="Size"><MonospaceValue>{formatBytes(artifact.size_bytes)}</MonospaceValue></Field><Field label="Created"><TimestampValue value={artifact.created_at} /></Field></dl></div><Button loading={pending} leadingIcon={<Download aria-hidden="true" className="size-4" />} onClick={onDownload}>{pending ? 'Preparing download' : `Download ${artifact.format.toUpperCase()}`}</Button></div><div className="mt-4 border-t border-border-subtle pt-4"><HashValue label="REPORT ARTIFACT SHA-256" value={artifact.content_hash} /><p className="mt-2 text-xs leading-5 text-text-muted">This digest verifies the stored rendered report artifact. It is separate from every source evidence acquisition or verification hash.</p></div></article> }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div className="min-w-0"><dt className="text-xs font-semibold uppercase tracking-wide text-text-muted">{label}</dt><dd className="mt-1 min-w-0 break-words text-sm text-text-secondary">{children}</dd></div> }
