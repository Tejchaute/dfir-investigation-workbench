import { ArrowRight, FileText } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Badge, HashValue, MonospaceValue, StatusBadge, TimestampValue } from '..'
import type { ReportRecord } from '../../types'
import { formatBytes } from '../../utils'

export function ReportRow({ report }: { report: ReportRecord }) { const artifact = report.artifacts[0]; return <li><Link to={`/cases/${report.case_id}/reports/${report.id}`} className="group grid min-w-0 gap-3 rounded-md border border-border-subtle bg-surface-base p-3 transition-colors hover:border-accent/50 hover:bg-surface-raised focus-visible:border-focus md:grid-cols-[minmax(0,1fr)_auto]"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><FileText aria-hidden="true" className="size-4 text-accent" /><MonospaceValue className="font-semibold">{report.report_type}</MonospaceValue><StatusBadge status={report.status} />{artifact && <Badge>{artifact.format}</Badge>}</div><div className="mt-3 grid gap-2 text-sm sm:grid-cols-2"><span className="text-text-muted">Report version <MonospaceValue>{report.report_version}</MonospaceValue></span><span className="text-text-muted">Generated by <span className="text-text-secondary">{report.generated_by}</span></span>{artifact && <span className="text-text-muted">Artifact size <span className="font-mono text-text-secondary">{formatBytes(artifact.size_bytes)}</span></span>}</div>{artifact && <div className="mt-3 min-w-0 overflow-hidden"><HashValue label="Report artifact SHA-256" value={artifact.content_hash} /></div>}</div><div className="flex min-w-0 items-center gap-3 md:justify-end"><div className="min-w-0 md:text-right"><TimestampValue value={report.generated_at} /><MonospaceValue className="mt-2 block max-w-48 truncate text-xs" title={report.id}>{report.id}</MonospaceValue></div><ArrowRight aria-hidden="true" className="size-4 shrink-0 text-text-muted transition-transform group-hover:translate-x-0.5 motion-reduce:transform-none" /></div></Link></li> }
