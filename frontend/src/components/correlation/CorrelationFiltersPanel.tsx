import type { FormEvent } from 'react'
import { RotateCcw, SlidersHorizontal } from 'lucide-react'
import { Button, FilterBar } from '..'
import type { CorrelationFilters } from '../../types'
const control = 'mt-1 h-9 w-full min-w-0 rounded-md border border-border-subtle bg-surface-inset px-3 font-mono text-xs text-text-primary hover:border-border-strong focus:border-accent'
export function CorrelationFiltersPanel({ value, onChange, onApply, onClear, disabled }: { value: CorrelationFilters; onChange: (value: CorrelationFilters) => void; onApply: () => void; onClear: () => void; disabled?: boolean }) { function submit(event: FormEvent) { event.preventDefault(); onApply() } return <form onSubmit={submit}><FilterBar><div className="grid min-w-0 flex-1 gap-3 sm:grid-cols-2"><label className="min-w-0 text-xs font-semibold text-text-muted">Rule ID<input className={control} value={value.ruleId ?? ''} onChange={(event) => onChange({ ...value, ruleId: event.target.value || undefined })} placeholder="Exact rule ID" /></label><label className="min-w-0 text-xs font-semibold text-text-muted">Correlation run ID<input className={control} value={value.correlationRunId ?? ''} onChange={(event) => onChange({ ...value, correlationRunId: event.target.value || undefined })} placeholder="UUID" /></label></div><div className="flex shrink-0 gap-2 self-end"><Button type="button" variant="ghost" leadingIcon={<RotateCcw aria-hidden="true" className="size-4" />} disabled={disabled} onClick={onClear}>Clear</Button><Button type="submit" leadingIcon={<SlidersHorizontal aria-hidden="true" className="size-4" />} disabled={disabled}>Apply filters</Button></div><p className="basis-full text-xs text-text-muted">Filters are exact matches supported by the correlation API. No severity, confidence, or threat filters are inferred.</p></FilterBar></form> }
