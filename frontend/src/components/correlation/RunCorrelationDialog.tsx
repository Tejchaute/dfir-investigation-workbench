import { GitCompareArrows } from 'lucide-react'
import { Button, Dialog } from '..'
export function RunCorrelationDialog({ open, onOpenChange, pending, onRun }: { open: boolean; onOpenChange: (open: boolean) => void; pending: boolean; onRun: () => void }) { return <Dialog open={open} onOpenChange={onOpenChange} title="Run deterministic correlation" description="Evaluate persisted timeline observations using backend-managed rules."><div className="space-y-4"><p className="text-sm leading-6 text-text-secondary">The backend will evaluate persisted TimelineEvents using its configured deterministic correlation rules. The frontend does not recreate, score, or alter those rules.</p><p className="text-xs text-text-muted">This is not AI, machine learning, threat prediction, or proof of causation. No progress percentage is inferred.</p><div className="flex justify-end gap-2"><Button variant="ghost" disabled={pending} onClick={() => onOpenChange(false)}>Cancel</Button><Button loading={pending} leadingIcon={<GitCompareArrows aria-hidden="true" className="size-4" />} onClick={onRun}>{pending ? 'Running correlation' : 'Run correlation'}</Button></div></div></Dialog> }
